# Render in-repo configuration for deploying this Vite static site
services:
  - type: web
    name: new-portfolio-2026
    branch: main
    buildCommand: "npm ci && npm run build"
    startCommand: "node server.js"
    envVars:
      - key: PORT
        value: "10000"
